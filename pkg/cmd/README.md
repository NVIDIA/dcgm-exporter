# pkg/cmd

Importable CLI and application wiring for dcgm-exporter: flags, environment
variables, defaults, startup, reload, logging, web config, and optional pprof.

The thin `package main` entrypoint lives in [`cmd/dcgm-exporter`](../../cmd/dcgm-exporter).
See [AGENTS.md](AGENTS.md) for change guidance.
